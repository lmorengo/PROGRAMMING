setwd("/Users/LaureanoMatías/Documents/R WORKING DIRECTORY") 
dir()
getwd
## FUNCTION POLLUTANTMEAN
pollutantmean <- function(directory, pollutant, id=1:332) {
    files_list <- list.files(directory, full.names = TRUE)
    datos <- data.frame()
    for (i in 1:332) {
        # loops through the files, rbinding them together
        datos <- rbind(datos, read.csv(files_list[i]))
    }
    datos_subset <- datos[which(datos[, "ID"] %in% id), ]
    mean(datos_subset[, pollutant], na.rm = TRUE)
}
##Example output from this function.
pollutantmean("specdata", "nitrate", 23)
pollutantmean("specdata", "sulfate", 1:10)
pollutantmean("specdata", "nitrate", 70:72)
